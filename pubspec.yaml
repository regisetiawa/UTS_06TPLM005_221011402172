name: weather
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Menambahkan folder assets untuk gambar latar belakang
  assets:
    - assets/images/background.jpg # Gambar untuk latar belakang
    # Anda dapat menambahkan lebih banyak gambar dengan format seperti di bawah ini:
    # - assets/images/other_image.png
